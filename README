# xv6-loongarch

本项目是在 MIT xv6 基础上完成的 **LoongArch 版本移植**，目标平台为 **QEMU virt** 机器模型。当前代码基线已经可以完成从启动到用户态程序运行的主线流程，适合作为课程项目、移植实验或毕业设计代码基础。

## 项目功能

当前仓库已经实现并验证了以下能力：

- xv6 内核可在 **LoongArch QEMU virt** 平台启动并进入 shell。
- 已打通启动、trap、外部中断、系统调用、进程调度、用户态切换等核心路径。
- 已支持基础文件系统，并把块设备后端切换为 **PCI virtio-blk**。
- 已适配并可运行 `usertests`，支持基础回归验证。
- 默认以 `-nographic` 方式运行，便于终端内调试与自动化测试。

当前 `Makefile` 的默认运行配置：

- 机器模型：`qemu-system-loongarch64 -machine virt`
- 内存大小：`256M`
- CPU 数量：`1`
- 磁盘后端：`virtio-blk-pci-non-transitional`

## 目录结构

- `kernel/`：内核源码，包括启动、内存管理、trap、调度、文件系统、磁盘驱动等
- `user/`：用户态程序与 `usertests`
- `mkfs/`：文件系统镜像构建工具
- `tools/tests/`：QEMU 自动化测试脚本
- `env.sh`：Linux 环境安装、工具链下载与环境激活脚本

## 运行环境

推荐在 Linux 主机上构建和运行。本项目已在以下环境下核对过命令可用性：

- 宿主机架构：`x86_64`
- QEMU：`10.1.2`
- LoongArch 交叉编译器：`loongarch64-unknown-linux-gnu-gcc 15.1.0`

### 脚本支持范围

`env.sh` 当前会优先支持以下 Linux 发行版的自动安装：

- Debian / Ubuntu：通过 `apt-get`
- Fedora：通过 `dnf`
- Arch Linux：通过 `pacman`

如果你的系统不在这三类里，也仍然可以手动安装依赖，然后执行 `source ./env.sh` 做环境激活和检查。

### 必需工具

需要保证以下工具可用：

- `gcc`：用于构建宿主侧 `mkfs`
- `make`
- `python3`
- `perl`
- `xxd`
- `qemu-system-loongarch64`
- `loongarch64-unknown-linux-gnu-gcc`
- `loongarch64-unknown-linux-gnu-ld`
- `loongarch64-unknown-linux-gnu-objdump`
- `loongarch64-unknown-linux-gnu-objcopy`

说明：

- 如果宿主机本身是 `loongarch64`，`Makefile` 会优先使用本机工具链。
- 如果宿主机是 `x86_64` 或 `aarch64`，`./env.sh install` 会自动从 `loong64/cross-tools` 的最新 release 下载 `loongarch64-unknown-linux-gnu` 工具链。
- 默认下载目录是 `~/.local/opt/x-tools/loongarch64-unknown-linux-gnu`。
- 如果你已经有自己的工具链，或想改安装位置，可以先设置：

```bash
export XV6_TOOLCHAIN_DIR=/path/to/your/toolchain
source ./env.sh
```

## 快速开始

### 1. 安装 Linux 依赖与工具链

建议先在项目根目录执行：

```bash
cd <repo-root>
./env.sh install
```

这一步会自动完成：

- 安装宿主机依赖，例如 `gcc`、`make`、`python3`、`perl`、`xxd`
- 安装或检查 `qemu-system-loongarch64`
- 下载 `loongarch64-unknown-linux-gnu` 交叉工具链到用户目录

如果系统已装好这些工具，脚本会自动跳过已有项。

### 2. 激活环境

```bash
source ./env.sh
```

这一步会把工具链加入当前 shell 的 `PATH`，并再次检查编译、运行和测试所需命令是否齐全。

### 3. 编译项目

```bash
make -j"$(nproc)"
```

默认目标会生成：

- `kernel/kernel`
- `fs.img`

如果只想先检查核心编译链路，也可以执行：

```bash
make -j2 kernel/kernel user/_usertests
```

### 4. 启动 xv6

```bash
make qemu
```

启动后会进入 xv6 shell。退出 QEMU 可使用 `Ctrl-A` 后按 `x`。

## 常用命令

### 正常运行

```bash
make qemu
```

### GDB 调试

启动 QEMU 并等待 GDB 连接：

```bash
make qemu-gdb
```

另开终端连接：

```bash
source ./env.sh
make gdb
```

### 快速运行 usertests

```bash
python3 tools/tests/xv6_test.py usertests --quick
```

### 运行单个 usertests 用例

```bash
python3 tools/tests/xv6_test.py usertests --case sbrkmuch
```

### 运行磁盘中断稳定性回归

```bash
make regress-diskirq
```

等价脚本命令为：

```bash
python3 tools/tests/xv6_test.py diskirq --rounds 10
```

## 从零运行的一套推荐流程

```bash
cd <repo-root>
./env.sh install
source ./env.sh
make clean
make -j"$(nproc)"
make qemu
```

## 说明

- 当前仓库基线已经不是“只完成部分移植”的状态，而是一个可启动、可运行用户程序、可执行基础测试的 LoongArch xv6 版本。
- 默认文件系统镜像 `fs.img` 会在构建时重新生成，并打包 `README` 与 `user/` 下的用户程序。
- 当前分支最近基线提交为 `a974fb0`：`review: tighten boot, vm, proc, fs, syscall, and disk paths`。
